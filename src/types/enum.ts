export enum Animations {"FadeIn"};
export enum Orientations { "horizontal", "vertical" };
export enum Positions { topLeft, topRight, bottomLeft, bottomRight, middleLeft, middleRight};
export enum Rounded { xs, sm, md, lg, xl, xxl, xxxl, full };
export enum Sides { "top", "right", "bottom", "left" }
export enum Sizes { xs, sm, md, lg, xl, xxl, xxxl };
export enum Tags { "div", "button", "a" }; // TODO update
export enum Templates { home, blog, category }; // TODO update
export enum Themes { default, green, blue };
export enum ThemesMode { dark, light, system }; // TODO Update