// import { Sizes } from "./const";
import {
  Rounded,
  Sizes,
  Templates,
  Themes,
  ThemesMode
} from "./enum";
// import { RequestState } from "./type";

const Types = {
  Rounded, // Enum type
  Sizes, // Enum type
  Templates, // Enum type
  Themes, // Enum type
  ThemesMode, // Enum type
  // RequestState // Types type
}

export default Types;