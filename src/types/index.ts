// import { Sizes } from "./const";
import {
  Animations,
  Orientations,
  Rounded,
  Sides,
  Sizes,
  Tags,
  Templates,
  Themes,
  ThemesMode
} from "./enum";
// import { RequestState } from "./type";

const Types = {
  Animations, // Enum type
  Orientations, // Enum type
  Rounded, // Enum type
  Sides, // Enum type
  Sizes, // Enum type
  Tags, // Enum type
  Templates, // Enum type
  Themes, // Enum type
  ThemesMode, // Enum type
  // RequestState // Types type
}

export default Types;